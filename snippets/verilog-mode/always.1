#name : always, using posedge
#key : always
# --
always @(posedge ${1:clk_apb} or negedge ${2:rst_apb_n }) begin
    if($2 == 1'b0) begin
      /*AUTORESET*/
    end
    else begin
      $0
    end
end
